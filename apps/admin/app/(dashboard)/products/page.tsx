import ComingSoon from '@/components/coming-soon';

export default function ProductsPage() {
  return (
    <ComingSoon
      label="products"
      title="产品管理"
      hint="下一阶段（phase 2）里把它接通。当前可以在 Claude Code 里直接用 muiad_register_product 登记产品。"
    />
  );
}
