import ComingSoon from '@/components/coming-soon';

export default function AdsPage() {
  return (
    <ComingSoon
      label="ads"
      title="广告管理"
      hint="下一阶段（phase 2）里把它接通。当前可以在 Claude Code 里直接用 muiad_create_ad 创建广告。"
    />
  );
}
